#!/usr/bin/env ruby
# TODO: Remove this
$LOAD_PATH.unshift File.join(File.dirname(__FILE__), '..', 'lib')

module Calabash
  class CLI
    require File.join(File.dirname(__FILE__), 'calabash_helpers')

    def initialize(arguments)
      @arguments = arguments
    end
  end
end

Calabash::CLI.new(ARGV.dup)
